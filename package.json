{
  "name": "fhir-patient-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Patient management app that reads/writes a FHIR R4 server through a local proxy (no mock data, no local state).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
